<!DOCTYPE html>
<html>
<head>
	<title>+EVlive Initial App | <%= @title %></title>
	<%= csrf_meta_tag -%>
	<%= render 'layouts/stylesheets' -%>
</head>

<body>
<div class="container">
	<%= render 'layouts/header' -%>
	<section class="round">
	<%= yield -%>
	</section>
	<%= render 'layouts/footer' -%>
	
	<!--	Debug appies only to 'development' environment	-->
		<%= debug(params) if Rails.env.development? -%>
	<!--	as determined by "if Rails.env.development?"	-->
</div><!-- / .container -->
</body>
</html>
